<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fun.project.mapper.UserMapper">

    <resultMap id="UserResult" type="User">
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="login_name" property="loginName"/>
        <result column="password" property="password"/>
        <result column="create_time" property="createTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="update_by" property="updateBy"/>
    </resultMap>

    <sql id="UserResultVo">
        select user_id,username,login_name,password,create_time,create_by,update_time,update_by,status from t_user
    </sql>

    <select id="selectUserById" parameterType="Long" resultMap="UserResult">
        <include refid="UserResultVo"/>
        WHERE user_id = #{userId}
    </select>

    <select id="selectUserList" parameterType="User" resultMap="UserResult">
        <include refid="UserResultVo"/>
        <where>

        </where>
    </select>

    <select id="login" parameterType="User" resultMap="UserResult">
        <include refid="UserResultVo"/>
        where login_name = #{loginName}
    </select>

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true">
        INSERT INTO t_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null  and userId != ''">user_id,</if>
            <if test="username != null  and username != ''">username,</if>
            <if test="loginName != null  and loginName != ''">login_name,</if>
            <if test="password != null  and password != ''">password,</if>
            <if test="createBy != null  and createBy != ''">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null  and userId != ''">#{userId},</if>
            <if test="username != null  and username != ''">#{username},</if>
            <if test="loginName != null  and loginName != ''">#{loginName},</if>
            <if test="password != null  and password != ''">#{password},</if>
            <if test="createBy != null  and createBy != ''">#{createBy},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE t_user
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null  and userId != ''">user_id = #{userId},</if>
            <if test="username != null  and username != ''">username = #{username},</if>
            <if test="loginName != null  and loginName != ''">login_name = #{loginName},</if>
            <if test="password != null  and password != ''">password = #{password},</if>
            update_time = sysdate(),
            <if test="updateBy != null  and updateBy != ''">update_by = #{updateBy}</if>
        </trim>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUserById" parameterType="Long">
        DELETE FROM t_user WHERE  user_id = #{userId}
    </delete>

    <delete id="deleteUserByIds" parameterType="String">
        delete from t_user where user_id in
        <foreach item="item" collection="array" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <select id="checkLoginNameUnique" resultType="Integer" parameterType="String">
        select count(1) from t_user where login_name = #{loginName}
    </select>

</mapper>
